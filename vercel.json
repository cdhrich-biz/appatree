{
  "buildCommand": "vite build && node build-api.mjs",
  "outputDirectory": "dist/public",
  "installCommand": "pnpm install",
  "functions": {
    "api/trpc/[trpc].js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/trpc/:path*", "destination": "/api/trpc/[trpc]" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
