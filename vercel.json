{
  "buildCommand": "vite build && node build-api.mjs",
  "installCommand": "pnpm install",
  "outputDirectory": "dist/public",
  "routes": [
    { "src": "/api/trpc/(.*)", "dest": "/api/trpc/[trpc].js" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
